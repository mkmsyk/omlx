# 管理画面アクティブモデル一覧の未コミット変更

## 背景

管理画面のアクティブモデル一覧に、モデル設定で非表示にしたモデルが残る未コミット変更があったため、実装と対応テストを確認してリポジトリの運用へ戻す。

## 変更

- `omlx/admin/routes.py` でモデル設定の `is_hidden` を取得し、非表示モデルを一覧から除外する。
- 除外対象でも、エンジンプール由来のメモリ統計と全体リクエスト集計は維持する。
- `tests/test_active_models_visibility.py` に、非表示モデルが一覧から消え、メモリ統計が残るケースを追加した。

## 検証

- 対象Pythonファイルの `compileall`: 成功。
- pytestによる実行は、ローカル環境に `pytest` と `fastapi` が無いため未実行。

## 残TODO

- oMLXの開発依存関係を導入したApple Silicon環境で、対象テストと高速テスト一式を実行する。
