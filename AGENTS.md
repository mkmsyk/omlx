# AGENTS.md

oMLX リポジトリの AI 用作業入口。ここには常時適用するルール・手順・環境情報だけを置き、製品仕様・個別機能の計画・検証記録・過去の経緯は `docs/` に置く。
すべての応答は日本語で行う。

## 0. 作業開始ゲート

- 作業開始時に、この `AGENTS.md` を先頭から末尾まで読む。
- 開発内容を確認するときは、まず `docs/INDEX.md` を確認し、索引 → 総則 → 各論の順に必要な文書だけを読む。`docs/INDEX.md` が無い場合は、文書を追加する作業で作成する。
- 仕様・API・依存関係・テストコマンドなど、実体で確認できる事実はコード、設定、`pyproject.toml`、テストを優先し、docs だけを根拠に断定しない。

## 1. 開発文書の運用

- 文書は `~/.claude/docs-management-standard.md` の三層構造に従う。
- ルート `AGENTS.md` は常時ルールだけに保ち、総則は `docs/<product>/<product>.md`、個別仕様・計画・検証記録・タスクリストは `docs/<product>/<product>-<feature>.{spec,plan,walkthrough,tasks}.md` に置く。
- 進行中の作業はリポジトリルートの `implementation_plan.md`、`walkthrough.md`、`task.md` を使い、完了時に `docs/` の各論へアーカイブしてルートのファイルを空テンプレートへ戻す。
- 完了済み計画、検証記録、事故記録、撤回済みの経緯、過去の実測値は `docs/archive/` に隔離し、`docs/INDEX.md` の通常の本表には混ぜない。
- 実質的な変更（機能追加、バグ修正、仕様・設定変更、複数ファイル変更、引き継ぎ）を行ったら、背景・設計判断・検証結果・残 TODO を `docs/internal/session-<topic>-<YYYYMMDD>.md` に記録し、`docs/INDEX.md` の内部作業ログへ追加する。
- 仕様書は現況だけを記述し、過去の状態や経緯はアーカイブまたは git 履歴へ分離する。

## 2. Git・完了条件

- ソース、設定、テスト、テンプレート、仕様書、計画書、検証記録を変更したら、意味のある一区切りごとに `git commit` して直後にユーザー側の書き込み可能なリモートへ push する。未コミット・未プッシュのまま作業を終えない。ユーザーがローカルのみ等を明示した場合だけ従う。
- `git add -A`、`git add .`、`git commit -a` は使わず、`git status` で今回触ったパスを確認し、`git add <path...>` で明示列挙する。他作業の変更を混ぜない。
- コミットメッセージは `type(scope): summary` 形式にする。コミット前に秘密情報が差分へ混入していないことと `git diff --check` を確認する。
- このチェックアウトでは `origin` が upstream、`fork` がユーザー側リポジトリの場合がある。upstream へ push せず、現在ブランチの追跡先またはユーザー側の書き込み可能なリモートを確認して push する。リモート構成を推測で変更しない。
- 各ターンの完了報告前に `git status` と upstream との差分を確認し、今回の変更を未コミット・未プッシュで残さない。
- 削除、履歴改変、force push、秘密情報更新、公開・課金などの不可逆操作は、ユーザーの明示承認なしに行わない。
- すべてのファイルを UTF-8（BOM なし）で維持する。

## 3. 計画・検証

- 複雑な変更は、着手前にルートの `implementation_plan.md` または対象 `docs/` の計画書へ記録し、検証結果を `walkthrough.md` または各論の walkthrough に残す。
- 通常の高速検証は `pytest -m "not slow"`、変更箇所の確認は対象テストを指定して行う。モデルファイルや起動中サーバーを必要とするテストは、対象であることを確認してから実行する。
- Python ソースを変更した場合は、利用可能な依存環境で対象テスト、構文検査、必要に応じて lint を実行し、依存不足や未実行の範囲を検証記録に明記する。

## 4. このファイルの役割

このファイルは作業開始ゲートである。具体的なモデル対応、API、管理画面、スキーマ、性能値、過去の障害や実装経緯はここへ蓄積せず、`docs/INDEX.md` から正本へ辿る。
